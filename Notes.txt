#include <iomanip>
#include <algorithm>
#include <iostream>
using namespace std;

int main()
{
	int x;
	cin >> x;
	cout << fixed << setprecision(9) << x << endl;
                                           
        z = min(x,y);
	y = max(x,y;

        // max number of pizza or ball  (formula = (n*(n+1)/2)+1);

	return 0;
}